# HANDOFF — state as of 2026-07-20 (read me first, then CLAUDE.md)

## Where the product stands
- Phase 1 code complete through **P1-F** (commit `8a5445f`, then `2b92c67`): engine (AR/FR/EN booking, 89/90 tests green), dashboard SPA (wizard/inbox/takeover/appointments/KB/settings), auth + tenant-scoped API + SSE, outbound sender (real+mock), notifications + emergency/hot-lead detectors wired end-to-end, deploy kit under `deploy/`.
- `npm test` → 89 pass / 0 fail / 1 skip (PG suite skips without DATABASE_URL — expected). `npm run simulate` → AR + FR bookings complete offline. Keep both green.

## Live WhatsApp wiring (REAL, done today — do not redo blindly)
- Meta app: **omen-clinic-agent**, app id `1525491438707612`, business portfolio `nitrox.69` (id `174044500164654`). A proper "OmenLabz" portfolio is blocked by a payment-account flag on the founder's FB account — appeal pending; re-home the app later.
- WABA id `1038353382027655`, **test number +1 (555) 177-7574**, phone_number_id `1153135121224452`. First tenant (El Amen) is mapped to it in `data/clinics.json` (committed).
- `.env` (git-ignored) holds: fresh 24h user token, phone number id, `WHATSAPP_VERIFY_TOKEN=<redacted-verify-token>`, APP_SECRET. **The token expires ~24h** — regenerate in the Meta console (Étape 1 page → Générer un token) and paste into `.env`, restart.
- Webhook registered to the founder's local tunnel: `https://dock-flooring-comparisons-issn.trycloudflare.com/webhook` (quick tunnels change URL each run → re-register in console Étape 2 → Configurer des webhooks when it changes).
- Recipient allow-list contains the founder's number `+216 29 496 305` (max 5 numbers on a test line).
- **Gotcha we hit:** the console's auto-subscribe silently failed — the app was NOT subscribed to the WABA, so no webhooks delivered. Fixed via `POST /v25.0/1038353382027655/subscribed_apps` (returns `{"success":true}`; verify with GET). If webhooks ever go silent, check this FIRST.
- Last known state: end-to-end live test (patient message → bot reply on real WhatsApp) initiated but not yet confirmed by the founder. If it hasn't been confirmed, verifying that loop is TASK ZERO: send `مرحبا نحب نحجز موعد` to the test number, watch `data/runtime/` for the conversation, check server logs for the send result, fix what breaks (usual suspects: expired token → 401; stale tunnel URL; subscription above).

## Priority backlog (in order — plan first, one slice at a time)
1. **P1-G — Postgres primary:** make the engine request path async on the store interface so `DATABASE_URL` flips production to PG end-to-end (adapter + schema + migrate/seed already exist and are tested). Keep all engine tests green; document cutover in RUNBOOK.
2. **Engine ↔ KB/persona integration:** the dashboard writes `kb_entries` (prices `source:'price'`, Q&A `answer:{ar,fr,en}`) and `config.persona`, but the engine doesn't read them yet. Make FAQ/pricing answers pull from the tenant KB, greeting/persona from config, with safe fallbacks. Tests.
3. **LLM quality pass:** per-tenant system prompt (KB-injected) for `anthropicProvider`, Libyan/Tunisian Arabic tone, strict guardrails in the prompt (no diagnosis, "from" prices only, emergency override stays deterministic). Offline mock behavior unchanged.
4. **Media intake (X-rays/photos):** webhook media messages → download via Graph → store under per-tenant media dir → surface in inbox/lead card → owner notification. Size/type limits, purge policy.
5. **Reminder templates (Phase 2 start):** template manager + T-48h/T-3h reminders with confirm/cancel buttons, respecting the 24h-window rules.
6. **Production hardening:** deploy to the founder's VPS per `deploy/RUNBOOK.md` (PM2+Nginx+TLS, real domain webhook), permanent System-User token path (§E), then real number when business verification clears.

## Operating rules (non-negotiable — also in CLAUDE.md)
Plan before code. Complete files, never fragments. Every query tenant-scoped. Engine stays transport-agnostic and must keep working offline with zero env. Never weaken medical guardrails. Commit per slice with clear messages. If sandbox/git ever blocks `unlink`, rename stale `.git/*.lock` files aside instead of deleting.
